[package]
name = "gain"
version = "0.1.0"
authors = ["Micah Johnston <micah@glowcoil.com>"]
edition = "2018"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
coupler = { path = ".." }
window = { path = "../window" }
graphics = { path = "../graphics" }
raw-window-handle = "0.3.3"

[features]
vst3 = []
